import { MoldeTypePipe } from './molde-type.pipe';

describe('MoldeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MoldeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
